#!/bin/bash

if [[ $(/usr/bin/id -u) -ne 0 ]]; then
    echo "Not running as root"
    exit
fi

apt install debootstrap grub-pc

umount /dev/sdb*

mkfs.ext4 /dev/sdb

mkdir usb

mount -o dev,exec,suid /dev/sdb usb

debootstrap bionic usb

grub-install --force --removable /dev/sdb

umount /dev/sdb
